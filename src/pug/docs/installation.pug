extends ../_docs-template

block vars
  - var title = 'Installation | Zalo Mini Program Components';

block content
  .docs-nav
    include ../_docs-menu
  .docs-content

    h1 Installation
    ul.docs-index

    h2 ZaUI CLI
    p The most recommended way to start with new Zalo Mini Program is to use <a href="/cli/">ZaUI CLI</a>, it provides most complete amount of combinations with different templates, target devices/platforms, frameworks and some of best practices.

    h2 Ready To Use Templates
    p You can also check the #[a(href="/templates/") starter app templates and tools] section to start straight away from some pre-setup app.

    h2 Manual Installation
    p We can download (install) ZaUI from NPM:
    :code
      $ npm install zmp


